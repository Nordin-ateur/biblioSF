{% extends 'base.html.twig' %}

{% block title %}Résultats de la recherche{% endblock %}

{% block titre %}Résultats de la recherche de <code>{{ mot }}</code> :{% endblock %}

{% block body %}
    <div class="d-flex flex-wrap">
    {% for livre in livres %}
        {{ include("livre/vignette.html.twig") }}
    {% endfor %}
    </div>

{% endblock %}
